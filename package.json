{
  "dependencies": {
    "follow-redirects": "^1.15.2",
    "qrcode-reader": "^1.0.4"
  },
  "main": "preload.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
